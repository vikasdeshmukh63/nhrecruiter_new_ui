import { CreateJobPost } from 'src/sections/jobpost/create';

export const metadata = {
  title: 'Create Job Posts | NovelHire',
};

export default function CreateJobPostPage() {
  return <CreateJobPost />;
}
